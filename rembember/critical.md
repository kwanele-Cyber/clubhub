# 1. when running command first run to activate the environment
```bash
./clubhub-venv/Scripts/Activate.ps1
```

# 2. if the project does not have an app.py as an entry point program
use this as the parameter for commands require flask
```bash
flask --app entrypoint_name_py
```

# 3. 