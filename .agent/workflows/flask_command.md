---
description: How to run flask commands in the ClubHub project
---
// turbo-all

1. Activate the python environment and run the command with the correct entry point.
```powershell
.\clubhub-venv\Scripts\Activate.ps1
flask --app run [COMMAND_STUB]
```
